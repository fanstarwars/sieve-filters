[
  {
    "namespace": "exporSieveCredentials",
    "description": "Read-only access to IMAP/POP3 credentials and incomingServer attributes stored in Thunderbird. Used to bootstrap middleware Basic-auth and auto-derive the middleware base URL without re-prompting the user.",
    "functions": [
      {
        "name": "getImapPassword",
        "type": "function",
        "description": "Try to fetch the saved IMAP/POP3 password for the given WebExtension accountId. Returns the password string on success, or null when the password is not stored, when the account is not IMAP/POP3, or when any error occurs. Never throws and never opens a master-password prompt.",
        "async": true,
        "parameters": [
          {
            "name": "accountId",
            "type": "string",
            "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)."
          }
        ]
      },
      {
        "name": "getServerInfo",
        "type": "function",
        "description": "Read non-secret incomingServer attributes for the given WebExtension accountId: hostname, port, type (imap/pop3), username, hostnameOrIp. Returns null when the accountId is unknown, when there is no incomingServer, when the type is not imap/pop3, or when any error occurs. Never throws and never opens a master-password prompt. Used to auto-derive `https://${hostname}/sieve-proxy` for middleware co-located with the user's mail server.",
        "async": true,
        "parameters": [
          {
            "name": "accountId",
            "type": "string",
            "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)."
          }
        ]
      },
      {
        "name": "listLocalFilters",
        "type": "function",
        "description": "Enumerate local Thunderbird message filters (msgFilterRules.dat → nsIMsgFilterList) for the given WebExtension accountId, projecting each nsIMsgFilter into a JSON-serialisable structure (name, enabled, matchAll, searchTerms[], actions[]) suitable for migration into a Sieve combined-script. Resolves targetFolderUri to a server-relative IMAP path (e.g. `INBOX/Logistics`). Returns [] on any error / unknown account / non-IMAP. Never throws and never opens a master-password prompt.",
        "async": true,
        "parameters": [
          {
            "name": "accountId",
            "type": "string",
            "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)."
          }
        ]
      },
      {
        "name": "getServerCheckAllFolders",
        "type": "function",
        "description": "Read the per-server preference `mail.server.<serverKey>.check_all_folders_for_new` for the given WebExtension accountId. When true, Thunderbird issues an IMAP STATUS for every subscribed folder of the account on each get-mail tick — that is what makes the unread-counter update for sub-folders into which Sieve fileinto'd new mail. Returns { supported, enabled }. supported=false for non-IMAP, unknown account, or when the serverKey can't be resolved (then enabled=null). Never throws.",
        "async": true,
        "parameters": [
          { "name": "accountId", "type": "string", "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)." }
        ]
      },
      {
        "name": "setServerCheckAllFolders",
        "type": "function",
        "description": "Write the per-server preference `mail.server.<serverKey>.check_all_folders_for_new` for the given accountId. Returns { supported, enabled } reflecting the value actually stored (echoes input on success). supported=false silently for non-IMAP / unknown account / serverKey resolve failure. The change takes effect on the next get-mail tick (Thunderbird does not re-evaluate immediately).",
        "async": true,
        "parameters": [
          { "name": "accountId", "type": "string", "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)." },
          { "name": "enabled", "type": "boolean", "description": "Target value of the preference." }
        ]
      },
      {
        "name": "listCheckNewFolders",
        "type": "function",
        "description": "Walk all sub-folders of the IMAP account and return their CheckNew flag state plus light metadata used by the Options UI to render the per-folder list. For each folder: { path (server-relative, with leading '/'), name (localised display name), checkNew (current flag state), specialUse (array of TB special-use names — 'inbox','junk','trash',…), isInbox, isVirtual, isSubscribed }. Virtual search folders and the server root are excluded. Returns [] for non-IMAP / unknown account / errors. Never throws.",
        "async": true,
        "parameters": [
          { "name": "accountId", "type": "string", "description": "WebExtension MailAccountId." }
        ]
      },
      {
        "name": "setFolderCheckNew",
        "type": "function",
        "description": "Set or clear the `nsMsgFolderFlags::CheckNew` (0x20000000) flag on a single folder identified by accountId + path. Path matches MailFolder.path (browser.folders.query) — server-relative with leading '/'. Returns { supported, enabled } where enabled echoes the value actually persisted, or { supported:false, enabled:null } on resolve failure. Setting the flag overrides Thunderbird's special-use exclusions: even folders like Junk/Trash will be polled when their CheckNew flag is set. The change takes effect on the next get-mail tick.",
        "async": true,
        "parameters": [
          { "name": "accountId", "type": "string" },
          { "name": "path",      "type": "string", "description": "Server-relative folder path with leading '/' (matches MailFolder.path)." },
          { "name": "enabled",   "type": "boolean" }
        ]
      },
      {
        "name": "deleteLocalFilters",
        "type": "function",
        "description": "Delete local Thunderbird message filters (msgFilterRules.dat → nsIMsgFilterList.removeFilterAt + saveToDefaultFile) by name for the given WebExtension accountId. Used by the Manager «Импорт» dialog after the user opts in to «Удалить локальные после импорта». Matches by case-insensitive filterName. Returns { deleted, errors[] }. Never throws and never opens a master-password prompt; on resolve / list errors returns { deleted:0, errors:[{name:null,msg}] } so the caller can keep the import green.",
        "async": true,
        "parameters": [
          {
            "name": "accountId",
            "type": "string",
            "description": "WebExtension MailAccountId (same as nsIMsgAccount.key)."
          },
          {
            "name": "names",
            "type": "array",
            "items": { "type": "string" },
            "description": "Array of filterName values (as projected by listLocalFilters). Empty → no-op."
          }
        ]
      }
    ]
  }
]
